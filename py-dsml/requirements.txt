# Programmatic notebook execution (headless build/verify pipelines)
nbclient
nbformat

# Plotting
matplotlib
seaborn

# Serialization beyond pickle (lambdas, closures, model bundles)
dill

# Verification cells and build-script tests
pytest

# Figure rendering and measurement. pillow already arrives as a matplotlib
# dependency, so this install is a no-op -- it is declared because figure
# scripts import it directly, and a direct import should not rest on another
# package's dependency tree.
pillow
cairosvg
